# Operations docs

Placeholder. Deployment topology for Phase 0 is `deploy/docker-compose.yml` (see root README's "Running locally" section). Fuller ops docs (backup/restore, upgrade procedure, monitoring) start once there's a real deployable feature set beyond scaffolding.
