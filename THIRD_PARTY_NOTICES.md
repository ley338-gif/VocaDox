# Third-Party Notices

VocaDox incorporates the third-party software listed below. This file is
generated by hand from the machine-readable, regenerable source of truth
in `compliance/` — never invented or guessed:

- **Direct dependencies** (34 packages, both ecosystems): `compliance/dependency-inventory.yml`
- **Full transitive dependency tree** (426 resolved packages — every
  package actually installed, not just the direct/top-level ones):
  `compliance/dependency-inventory-transitive.yml`, regenerated from real
  `pip-licenses`/`license-checker` output against the actual installed
  trees via `compliance/generate_transitive_inventory.py`
- **Container images**: `compliance/container-inventory.yml`
- **AI models**: `compliance/model-inventory.yml` (4 entries as of Phase
  3.1 — none bundled in the product itself; each is admin-installed
  separately, see `docs/admin/model-installation.md`)

Run `python compliance/check_licenses.py` to re-verify all four
inventories against `compliance/license-policy.yml` at any time. Last
regenerated: 2026-08-18 (Phase 1 — added argon2-cffi and @types/node).

No dependency, container image, or model in any tier is licensed under a
copyleft-with-redistribution-obligation license (GPL/AGPL/SSPL/RSAL/BSL),
carries a commercial-use carve-out (Commons Clause), is proprietary, or is
of unverified/unknown license — see `compliance/license-policy.yml` for
the full policy and `docs/licenses/license-policy.md` for the rationale.

## Direct dependencies — Python (PyPI)

| Package | License | Role |
|---|---|---|
| fastapi | MIT | runtime |
| uvicorn | BSD-3-Clause | runtime |
| pydantic | MIT | runtime |
| pydantic-settings | MIT | runtime |
| sqlalchemy | MIT | runtime |
| alembic | MIT | runtime |
| asyncpg | Apache-2.0 | runtime |
| valkey | MIT | runtime |
| python-multipart | Apache-2.0 | runtime |
| argon2-cffi | MIT | runtime (Phase 1 — Argon2id password hashing) |
| setuptools | MIT | build-only (PEP 517 build backend, not imported at runtime) |
| ruff | MIT | dev-only (lint) |
| mypy | MIT | dev-only (typecheck) |
| pytest | MIT | dev/test-only |
| pytest-asyncio | Apache-2.0 | dev/test-only |
| httpx | BSD-3-Clause | dev/test-only |
| pip-licenses | MIT | compliance-tool-only, never shipped |
| pip-audit | Apache-2.0 | compliance-tool-only, never shipped |

## Direct dependencies — Node (npm)

| Package | License | Role |
|---|---|---|
| react | MIT | runtime |
| react-dom | MIT | runtime |
| react-router | MIT | runtime |
| @tanstack/react-query | MIT | runtime |
| zod | MIT | runtime |
| lucide-react | ISC | runtime |
| @fontsource/inter | OFL-1.1 (packages the SIL Open Font License 1.1-licensed Inter typeface) | runtime |
| vite | MIT | dev-only (build tool) |
| @vitejs/plugin-react | MIT | dev-only |
| typescript | Apache-2.0 | dev-only |
| eslint | MIT | dev-only |
| vitest | MIT | dev/test-only |
| @testing-library/react | MIT | dev/test-only |
| openapi-typescript | MIT | dev/codegen-only |
| license-checker | BSD-3-Clause | compliance-tool-only, installed with `--no-save`, never recorded in package.json/package-lock.json |
| @types/node | MIT | dev-only (type declarations for `vite.config.ts`'s dev-server proxy config, Phase 1) |

## Transitive dependency tree

426 resolved packages total across both ecosystems (the direct ones
above plus everything they in turn depend on). Full detail — package name,
version, ecosystem, license, direct/transitive flag, production/dev scope
— is in `compliance/dependency-inventory-transitive.yml`, not duplicated
here since it's too large for a readable table.

| Status | Count | Disposition |
|---|---|---|
| Approved | 424 | used freely |
| Review required | 2 | `certifi` and `pathspec`, both MPL-2.0, both dev-tooling-only transitive dependencies (of `pip-audit` and `mypy` respectively) — used entirely unmodified, never shipped in the backend Docker image. Explicit sign-off recorded in `compliance/exceptions.yml`. |
| Blocked | 0 | — |
| Unknown | 0 | — |

A handful of exotic-but-legitimate permissive/public-domain licenses
were discovered in the transitive tree and added to
`compliance/license-policy.yml`'s approved list with per-package
provenance notes (not silently waved through): `MIT-0`, `BlueOak-1.0.0`,
`CC0-1.0`, `CC-BY-3.0`, `CC-BY-4.0`, `PSF-2.0`. All are dev-tooling-only
transitive dependencies, none appear in a production runtime tree.

## Container base images

| Image | License | Shipped to customers? |
|---|---|---|
| postgres:16.6-alpine3.20 | PostgreSQL License | yes (datastore) |
| valkey/valkey:8.0.2-alpine | BSD-3-Clause | yes (queue/cache) |
| python:3.11-slim-trixie | PSF-2.0 (interpreter); mixed OS-package licenses (Debian base layer) | yes — backend runtime image |
| nginx:1.31.3-alpine3.24 | BSD-2-Clause (nginx license) | yes — frontend runtime image |
| node:22-alpine3.24 | MIT (Node.js runtime); mixed OS-package licenses (Alpine base layer) | no — frontend build/dev stage only, discarded by the multi-stage build |
| aquasec/trivy:0.56.2 | Apache-2.0 | no — vulnerability/SBOM scan tool only, never part of the deployed stack |

`ollama/ollama:0.33.2` was bundled here as a shipped local LLM inference
server through Phase 11. Removed as part of the Phase 12 GA-blocker fix
(CVE-2026-56854, an unfixed CRITICAL vendored-dependency finding) — see
`docs/architecture/adr/0029-remove-bundled-ollama.md`. VocaDox no longer
ships, builds, or scans this image; fact extraction now talks to an
admin-managed external Ollama instance (`docs/admin/llm-provider.md`),
whose own third-party notices are that admin's responsibility, not
VocaDox's.

## Fonts and icons

- **Inter** (self-hosted via `@fontsource/inter`, bundled at build time —
  no CDN/external font requests) — SIL Open Font License 1.1. See
  `docs/licenses/fonts-assets.md`.
- **Lucide** icon set (via `lucide-react`, bundled at build time) — ISC
  License. See `docs/licenses/fonts-assets.md`.

## Compliance / build tooling

Tools used only to build the application or verify its compliance —
never imported by application code, never present in a shipped runtime
image:

| Tool | License | Purpose |
|---|---|---|
| pip-licenses | MIT | Python license inventory generation |
| pip-audit | Apache-2.0 | Python dependency vulnerability scanning |
| license-checker | BSD-3-Clause | Node license inventory generation |
| Trivy (aquasec/trivy Docker image) | Apache-2.0 | Container SBOM + vulnerability scanning (chosen over Docker Scout, whose CLI is proprietary-licensed) |

## AI models

None bundled directly in any VocaDox image or repository — every
speech/diarization model is downloaded by an admin, on request, into a
persistent volume (see `docs/admin/model-installation.md`); the Phase 4
LLM model is pulled by an admin into their own, separately-run Ollama
instance's storage (see `docs/admin/llm-provider.md`) — no longer a
VocaDox-managed Compose volume as of the Phase 12 GA-blocker fix (see
`docs/architecture/adr/0029-remove-bundled-ollama.md`).
`compliance/model-inventory.yml` records 6 approved entries:

| Model | License | Gated? | Role |
|---|---|---|---|
| `Systran/faster-whisper-small` | MIT | No | Speech-to-text |
| `pyannote/speaker-diarization-3.1` | MIT | Yes | Diarization (top-level pipeline) |
| `pyannote/segmentation-3.0` | MIT | Yes | Diarization (segmentation sub-model) |
| `pyannote/wespeaker-voxceleb-resnet34-LM` | CC-BY-4.0 | No | Diarization (speaker-embedding sub-model) |
| `pyannote/speaker-diarization-community-1` (plda/ only) | CC-BY-4.0 | Yes | Diarization (unused-but-required PLDA transform) |
| `Qwen2.5-14B-Instruct` (via Ollama, GGUF) | Apache-2.0 | No | Fact extraction (Phase 4) |

---

For the exact version pinned, the license source verified, and any
per-package notes, see `compliance/dependency-inventory.yml`,
`compliance/dependency-inventory-transitive.yml`, and
`compliance/container-inventory.yml` — those files are the source of
truth this document is derived from.
